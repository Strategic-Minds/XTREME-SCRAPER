import WorkspacePage from '@/components/WorkspacePage'

export default function SettingsPage() {
  return <WorkspacePage title="Settings" eyebrow="Profile, organization, privacy" description="Configure the business context XPS uses for suggestions and scoring while keeping security and data controls explicit." cards={[{title:'Business profile',copy:'Industry, services, customer types, target regions, commercial goals, and scoring preferences.'},{title:'Organization access',copy:'Owner, admin, analyst, member, and read-only roles with least-privilege permissions.'},{title:'Privacy and retention',copy:'Export permissions, retention windows, deletion requests, audit history, and communication consent.'}]} emptyTitle="Secure settings require authentication" emptyCopy="Preview mode displays the contract but does not write profile or organization settings. Configure the preview JWT secret and approve the RLS migration first." notice="The previous page contained hard-coded example account data and a token-like value. Those unsafe placeholders have been removed." actionHref="/login" actionLabel="Open secure sign-in" />
}
