import SearchWorkspace from '@/components/SearchWorkspace'

export default function ResultsPage() {
  return <SearchWorkspace title="Results and Evidence" initialQuery="Find companies with source-backed contacts and current commercial signals" />
}
